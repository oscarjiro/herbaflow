// One home for the analysis shell-mode decision (spec default: "unified" — the setup form lives
// inside the run-shell chrome with no global top navbar). Flip to "standalone" to render the bare
// setup form with the global Nav/Footer instead.
export const SHELL_MODE: "unified" | "standalone" = "unified";
